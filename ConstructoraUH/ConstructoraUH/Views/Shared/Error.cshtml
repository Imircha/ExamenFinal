@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Ocurrió un error al procesar su solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Detalles del Desarrollo</h3>
<p>
    Cambiar a <strong>Desarrollo</strong> el ambiente muestra información detallada sobre el error que ocurrió.
</p>
<p>
    <strong>El ambiente de desarrollo no debe ser habilitado para aplicaciones desplegadas.</strong>
    Puede resultar en la visualización de información sensible de excepciones a los usuarios finales.
    Para depuración local, habilite el ambiente de <strong>Desarrollo</strong> configurando la variable de ambiente <strong>ASPNETCORE_ENVIRONMENT</strong> a <strong>Development</strong>
    y reiniciando la aplicación.
</p>